name: intellij
app: java@17

git:
  url: https://github.com/yuriuser126/intellij.git
  branch: main

build:
  buildsystem: gradle
  buildcommand: ./gradlew build

run:
  jarfile: build/libs/*.jar
  port: 8989
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: prod
